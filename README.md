# Kaneki Bot

Para instalar e rodar o bot, siga os comandos abaixo:

```bash
git clone https://github.com/polarminhapomba-crypto/kaneki-bot.git
cd kaneki-bot
npm run config
npm run config:install
npm start
```
